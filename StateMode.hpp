// StateMode.hpp
#pragma once

enum class StateMode {
	Idle,
	Run,
	Jump,
	Attack,
	Hurt,
	Dead
};
/*
enum class PlayerState { Idle, Jump, Attack };
enum class EnemyState { Idle, Patrol, Hurt };


*/
